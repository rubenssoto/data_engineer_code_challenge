[tool.poetry]
name = "infra-test"
version = "0.1.0"
description = ""
authors = ["Rubens"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
boto3 = "^1.35.5"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
